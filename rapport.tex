\documentclass[12pt,a4paper]{report}

% --- Packages de base ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{graphicx}
\usepackage{geometry}
\geometry{hmargin=2.5cm,vmargin=2.5cm}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{array}

% --- Configuration du code source ---
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    stringstyle=\color{red},
    commentstyle=\color{green!60!black},
    breaklines=true,
    numbers=left,
    numberstyle=\tiny,
    frame=single
}

% --- Informations du document ---
\title{Rapport de Projet IA - M1 TI \\ Classification de Séries Temporelles}
\author{LACATON Damien - BOUZOM Lukas}
\date{\today}

\begin{document}

\maketitle

\tableofcontents

\chapter{Introduction}

Ce projet fait partie de l'UE IA pour les systèmes cyber-physiques en Master 1. L'objectif est de faire de la classification de séries temporelles. Ces données représentent des traces d'activités de processeur (CPU), de disque et de mémoire vive (RAM). 

Le but est de pouvoir identifier automatiquement de quel type d'activité il s'agit à partir de traces d'exécution.

\section{Objectifs du projet}
Le travail consiste à tester et comparer trois approches différentes :
\begin{itemize}
    \item Les réseaux de neurones récurrents de type LSTM.
    \item Les réseaux de neurones convolutionnels (CNN 1D).
    \item Les modèles plus récents basés sur le mécanisme d'attention (Transformers).
\end{itemize}

Puis proposer des pistes susceptibles de répondre au problème identifié afin de déterminer si les séquences de traces d’exécution proviennent d’un navigateur web, d’un compilateur, d’un logiciel de traitement de texte, etc.

\chapter{Présentation et préparation des données}

\section{Origine des données}
Les données utilisées proviennent d'un dépôt GitHub public regroupant des traces d'exécution de CPU. Chaque échantillon correspond à un fichier CSV qui regroupe plusieurs colonnes de mesures du système, relevées à des intervalles réguliers.\\
\url{https://github.com/phuongntmse/ts-data-for-workload-classification}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/OrigineDonee.png}
    \caption{Benchmark Configuration Example}
    \label{fig:origine}
\end{figure}

\newpage
\section{Analyse de l'équilibre des classes}
Après avoir extrait toutes les séquences, nous avons analysé la répartition des données entre les trois catégories. On observe que les classes ne sont pas équilibrées : le CPU représente la majorité des données, alors que nous disposons de moins de données pour la mémoire et le disque

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/RepartitionDonee.png}
    \caption{Répartition des séquences par classe (CPU, Disque, Mémoire)}
    \label{fig:repartition}
\end{figure}

Ce déséquilibre important a une influence directe sur l'apprentissage. Les modèles auront tendance à mieux reconnaître la classe CPU car ils ont plus d'exemples pour apprendre sa signature.

\newpage
\section{Exploration visuelle}
Avant de traiter les données, nous avons visualisé les signatures de chaque catégorie. Par exemple, une activité intense du CPU correspond à une charge élevée et continue du processeur, tandis qu'une activité du disque montre des pics lors des phases de lecture ou d'écriture. Enfin, l'activité de la mémoire se manifeste par une occupation assez constante de la RAM, sans les variations brusques observées pour le disque.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/ComparaisonSignature.png}
    \caption{Comparaison des signatures CPU, Disque et Mémoire}
    \label{fig:signatures}
\end{figure}

\section{Prétraitement}
Pour que les modèles puissent apprendre efficacement, les données subissent plusieurs transformations :
\begin{itemize}
    \item \textbf{Sélection des colonnes} : Nous retenons l'usage global du CPU, l'occupation de la RAM et les taux de lecture/écriture disque.
    \item \textbf{Normalisation} : Les valeurs sont ramenées entre 0 et 1 pour éviter que les grandes unités (comme les octets de mémoire) n'écrasent les petites (pourcentages CPU).
    \item \textbf{Fenêtrage} : Les données sont découpées en séquences de 50 unités de temps. C'est ce bloc de 50 points qui sera donné en entrée aux modèles.
\end{itemize}

\chapter{Modèle 1 : LSTM (Long Short-Term Memory)}

\section{Théorie et fonctionnement}
Le LSTM est un type de réseau de neurones récurrent (RNN) conçu pour traiter des données séquentielles. Contrairement à un réseau classique, il possède une "mémoire" lui permettant de retenir des informations sur le long terme. Cette architecture est idéale pour nos traces système, car un pic d'activité CPU n'a de sens que s'il est analysé dans son contexte temporel.

Le fonctionnement repose sur des "portes" (gates) qui décident quelles informations passées doivent être conservées, mises à jour ou oubliées.

\section{Architecture implémentée}
Pour notre projet, nous avons construit un modèle avec les caractéristiques suivantes :
\begin{itemize}
    \item \textbf{Couche LSTM} : 64 unités pour capturer la dynamique des 4 variables d'entrée.
    \item \textbf{Dropout (20\%)} : Une sécurité pour éviter le sur-apprentissage en désactivant aléatoirement certains neurones pendant l'entraînement.
    \item \textbf{Couche Dense} : 32 neurones avec activation ReLU pour la prise de décision.
    \item \textbf{Couche de sortie} : 3 neurones avec activation Softmax pour obtenir une probabilité sur les classes (CPU, Disque, Mémoire).
\end{itemize}

L'entraînement a été réalisé sur 15 époques avec une taille de lot (batch size) de 64.

\section{Résultats et analyse}
Le modèle obtient une précision globale de \textbf{91\%}. Cependant, l'analyse détaillée montre des disparités selon les classes.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{capture/CourbeLSTM1.png}
    \caption{Courbe d'apprentissage du modèle LSTM (Accuracy et Loss)}
    \label{fig:courbe_lstm}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{capture/MatriceLSTM1.png}
    \caption{Matrice de confusion du modèle LSTM}
    \label{fig:matrice_lstm}
\end{figure}

\subsection{Performance par catégorie}
\begin{itemize}
    \item \textbf{Disque} : Le score est parfait (F1-score de 1.00). La signature des accès disque est très marquée et ne laisse aucune place à l'erreur.
    \item \textbf{CPU} : Très bien identifié avec un score de 0.92.
    \item \textbf{Mémoire} : C'est le point faible du modèle avec un rappel (recall) de seulement \textbf{0.59}. Une partie des activités mémoire est confondue avec le CPU. Cela s'explique par le fait que certaines opérations de gestion de la RAM sollicitent aussi le processeur d'une manière similaire à un calcul de fond.
\end{itemize}

\chapter{Modèle 2 : CNN 1D (Convolutional Neural Network)}

\section{Théorie et fonctionnement}
Habituellement utilisé pour l'analyse d'images (2D), le réseau de neurones convolutionnel (CNN) peut être adapté aux séries temporelles en utilisant des convolutions 1D. Au lieu de regarder l'image entière, le modèle fait glisser des filtres le long de la série temporelle pour détecter des motifs locaux (pics, transitions brusques, oscillations spécifiques).

L'avantage du CNN par rapport au LSTM est sa capacité à détecter des signatures de formes précises sans avoir besoin de "retenir" toute l'histoire de la séquence.

\section{Architecture implémentée}
Le modèle CNN 1D repose sur l'architecture suivante :
\begin{itemize}
    \item \textbf{Couche Conv1D} : 64 filtres avec une taille de noyau (kernel) de 3 pour extraire les caractéristiques locales.
    \item \textbf{MaxPooling1D} : Réduit la dimension temporelle en ne gardant que les valeurs les plus significatives de chaque zone.
    \item \textbf{Flatten} : Transforme les cartes de caractéristiques en un vecteur plat pour la classification finale.
    \item \textbf{Dense et Dropout} : Couches classiques pour l'interprétation finale et la régularisation.
\end{itemize}

\section{Résultats et analyse}
Le CNN 1D montre une légère amélioration par rapport au LSTM avec une précision globale de \textbf{92\%}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{capture/CourbeCNN1.png}
    \caption{Courbe d'apprentissage du modèle CNN 1D}
    \label{fig:courbe_cnn}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{capture/MatriceCNN1.png}
    \caption{Matrice de confusion du modèle CNN 1D}
    \label{fig:matrice_cnn}
\end{figure}

L'amélioration se situe principalement sur la classe \textbf{Mémoire}, où le rappel passe à \textbf{0.61}. Bien que la confusion avec le CPU persiste, les filtres de convolution semblent mieux isoler certaines signatures spécifiques aux accès RAM que le mécanisme de mémoire du LSTM.

\chapter{Modèle 3 : Time Series Transformer}

\section{Théorie et fonctionnement}
Le Transformer est l'architecture la plus récente testée dans ce projet. Contrairement aux modèles précédents qui traitent la séquence point par point (LSTM) ou par zones locales (CNN), le Transformer utilise un mécanisme d' \textbf{Attention}. Cela lui permet de calculer des relations entre n'importe quels points de la fenêtre de 50, même s'ils sont éloignés.

Dans notre cas, cela signifie que si un pic CPU à l'instant $t=5$ est lié à une chute de RAM à l'instant $t=45$, le modèle peut capturer cette dépendance directement.

\section{Architecture implémentée}
\begin{itemize}
    \item \textbf{Multi-Head Attention} : Utilisation de 4 têtes d'attention pour analyser les données selon différentes perspectives en parallèle.
    \item \textbf{Global Average Pooling} : Condense l'information de toute la séquence.
    \item \textbf{Normalisation par couche} : Stabilise l'apprentissage, ce qui est crucial pour les Transformers qui peuvent être instables lors de l'entraînement.
\end{itemize}

\section{Résultats et analyse}
Le Transformer obtient une précision de \textbf{89\%}. C'est le score le plus faible des trois approches.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{capture/CourbeTransformer1.png}
    \caption{Courbe d'apprentissage du modèle Transformer}
    \label{fig:courbe_transformer}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{capture/MatriceTransformer1.png}
    \caption{Matrice de confusion du modèle Transformer}
    \label{fig:matrice_transformer}
\end{figure}

L'analyse du rappel pour la classe \textbf{Mémoire} (0.59) montre que ce modèle rencontre les mêmes difficultés que le LSTM. Bien que très puissant sur des séquences très longues, le mécanisme d'attention globale n'apporte pas de gain sur nos fenêtres de 50 points par rapport à la simplicité des filtres du CNN 1D.

\chapter{Synthèse et Perspectives}

\section{Comparaison des modèles}
Le tableau ci-dessous résume les performances obtenues :

\begin{table}[h]
\centering
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Modèle} & \textbf{Précision Globale} & \textbf{Rappel (Mémoire)} & \textbf{F1-Score (CPU)} \\ \hline
LSTM            & 91\%                       & 0.59                      & 0.92                    \\ \hline
\textbf{CNN 1D} & \textbf{92\%}              & \textbf{0.61}             & \textbf{0.93}           \\ \hline
Transformer     & 89\%                       & 0.59                      & 0.90                    \\ \hline
\end{tabular}
\caption{Synthèse des performances comparées}
\end{table}

Le \textbf{CNN 1D} est le modèle le plus performant pour ce problème spécifique. Il offre le meilleur compromis entre détection des pics système et temps d'exécution.

\section{Perspectives : Vers une application réelle}
Pour passer de l'identification de "micro-tâches" (50 secondes) à l'identification d'une application entière (ex: Chrome, GCC), nous proposons une \textbf{approche hiérarchique} :

\begin{enumerate}
    \item \textbf{Niveau 1} : Notre CNN 1D actuel étiquette chaque bloc de 50 secondes. On obtient une séquence d'étiquettes du type $[CPU, CPU, DISK, MEMORY, \dots]$.
    \item \textbf{Niveau 2} : Un second modèle (léger) analyse cette "macro-séquence" d'étiquettes pour reconnaître la signature d'un logiciel complet.
\end{enumerate}

\section{Conclusion}
Ce projet a permis de démontrer l'efficacité du Deep Learning pour la supervision IT. Si le CNN 1D s'est montré le plus robuste, la confusion persistante entre CPU et Mémoire souligne la difficulté intrinsèque de distinguer ces deux activités qui partagent souvent des ressources communes au niveau physique.

\end{document}
