\documentclass[12pt,a4paper]{report}

% --- Packages de base ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{graphicx}
\usepackage{geometry}
\geometry{hmargin=2.5cm,vmargin=2.5cm}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{array}

% --- Configuration du code source ---
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    stringstyle=\color{red},
    commentstyle=\color{green!60!black},
    breaklines=true,
    numbers=left,
    numberstyle=\tiny,
    frame=single
}

% --- Informations du document ---
\title{Rapport de Projet IA - M1 TI \\ Classification de Séries Temporelles}
\author{LACATON Damien - BOUZOM Lukas}
\date{\today}

\begin{document}

\maketitle

\tableofcontents

\chapter{Introduction}

Ce projet fait partie de l'UE IA pour les systèmes cyber-physiques en Master 1. L'objectif est de faire de la classification de séries temporelles. Ces données représentent des traces d'activités de processeur (CPU), de disque et de mémoire vive (RAM). 

Le but est de pouvoir identifier automatiquement de quel type d'activité il s'agit à partir de traces d'exécution.

\section{Objectifs du projet}
Le travail consiste à tester et comparer trois approches différentes :
\begin{itemize}
    \item Les réseaux de neurones récurrents de type LSTM.
    \item Les réseaux de neurones convolutionnels (CNN 1D).
    \item Les modèles plus récents basés sur le mécanisme d'attention (Transformers).
\end{itemize}

Puis proposer des pistes susceptibles de répondre au problème identifié afin de déterminer si les séquences de traces d’exécution proviennent d’un navigateur web, d’un compilateur, d’un logiciel de traitement de texte, etc.

\chapter{Présentation et préparation des données}

\section{Origine des données}
Les données utilisées proviennent d'un dépôt GitHub public regroupant des traces d'exécution de CPU. Chaque échantillon correspond à un fichier CSV qui regroupe plusieurs colonnes de mesures du système, relevées à des intervalles réguliers.\\
\url{https://github.com/phuongntmse/ts-data-for-workload-classification}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/OrigineDonee.png}
    \caption{Benchmark Configuration Example}
    \label{fig:origine}
\end{figure}


\section{Exploration visuelle}
Avant de traiter les données, nous avons visualisé les signatures de chaque catégorie. Par exemple, une activité intense du CPU correspond à une charge élevée et continue du processeur, tandis qu'une activité du disque montre des pics lors des phases de lecture ou d'écriture. Enfin, l'activité de la mémoire se manifeste par une occupation assez constante de la RAM, sans les variations brusques observées pour le disque.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/ComparaisonSignature.png}
    \caption{Comparaison des signatures CPU, Disque et Mémoire}
    \label{fig:signatures}
\end{figure}

\newpage
\section{Préparation des Données}
Pour que les modèles puissent apprendre efficacement, il faut appliquer plusieurs étapes aux données :

\begin{itemize}
    \item \textbf{Sélection des données} : Nous retenons les 4 variables les plus intéressantes :
    \begin{enumerate}
        \item \texttt{CpuALL\_usage} : Charge globale du processeur.
        \item \texttt{memory\_usage\_perce} : Occupation de la RAM.
        \item \texttt{read\_rate\_during\_time} : Vitesse de lecture disque.
        \item \texttt{write\_rate\_during\_time} : Vitesse d'écriture disque.
    \end{enumerate}
    \item \textbf{Normalisation} : Afin de pouvoir comparer les valeurs plus facilement, nous les ramenons sur un intervalle commun entre 0 et 1 grâce à \textit{MinMaxScaler}. Cela permet l'unicité des données, en évitant que certaines valeurs soient exprimées en pourcentage comme l'activité CPU et d'autres en octets comme la mémoire ; on pourra donc se les représenter dans un même graphe afin de permettre un meilleur apprentissage des modèles.
    \item \textbf{Fenêtrage} : Les données sont découpées en séquences de 50 unités de temps. Pour les modèles LSTM et CNN, c'est crucial afin qu'ils capturent l'activité sur un intervalle de temps plutôt qu'à un instant T. C'est ce bloc de 50 points qui sera donné en entrée aux modèles.
    \item \textbf{Labels} : Pour pouvoir entraîner nos modèles, il faut étiqueter nos données ; pour cela, nous avons déterminé que \textbf{CPU = 0, DISQUE = 1 et MÉMOIRE = 2}.
\end{itemize}

L'image ci-dessous représente les données après leur préparation. On observe que les quatre variables sont désormais à la même échelle, ce qui permet de les comparer sur une fenêtre de 50 points.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/VisluationTraitement.png}
    \caption{Visualisation de fenêtres de 50 points après normalisation}
    \label{fig:traitement}
\end{figure}

La préparation des données nous a permis de constituer un dataset comportant :
\begin{itemize}
    \item 35 928 séquences
    \item 50 unités de longueur
    \item 4 variables
\end{itemize}
    
\newpage
\section{Analyse de la répartition des données}
Après avoir extrait toutes les séquences, nous avons analysé la répartition des données entre les trois catégories. On observe que les classes ne sont pas équilibrées : le CPU représente la majorité des données, alors nous disposons de moins de données pour la mémoire et le disque

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/RepartitionDonee.png}
    \caption{Répartition des séquences par classe (CPU, Disque, Mémoire)}
    \label{fig:repartition}
\end{figure}

Ce déséquilibre entre les données aura une influence sur l'apprentissage. Les modèles auront tendance à mieux reconnaître la classe CPU, car ils ont plus d'exemples pour apprendre sa signature.

\chapter{Modèle 1 : LSTM (Long Short-Term Memory)}

\section{Fonctionnement}
Le LSTM est un réseau de neurones utilisé pour traiter des données séquentielles. Ce type de réseau a la particularité de pouvoir retenir des informations sur le long terme. Cela va être très utile pour reconnaître un pic d'activité CPU, car il faut analyser dans un contexte temporel, ici sur un intervalle de 50 unités de temps.

Son fonctionnement consiste à décider quelles informations passées doivent être conservées, mises à jour ou oubliées, ce qui nous permettra de reconnaître aisément une trace qui s'étale dans le temps (ex : une lecture disque).

\section{Architecture}
Nous avons construit un modèle avec les caractéristiques suivantes :
\begin{itemize}
    \item \textbf{Entrée} : Des tenseurs sous la forme (50, 4).
    \item \textbf{Couche LSTM} : 64 unités pour capturer la dynamique des 4 variables.
    \item \textbf{Dropout (20\%)} : Pour éviter le sur-apprentissage, 20\% des neurones sont désactivés aléatoirement durant l'entraînement.
    \item \textbf{Couche Dense} : 32 neurones ReLU pour la prise de décision.
    \item \textbf{Sortie} : 3 neurones Softmax pour distribuer une probabilité pour chacune des 3 classes (CPU, Disque, Mémoire).
\end{itemize}

L'entraînement est réalisé sur 15 epochs et batch size de 64.

Avec les paramètres suivants :
\begin{itemize}
    \item \textbf{Optimiseur} : Adam
    \item \textbf{Fonction de perte} : \texttt{sparse\_categorical\_crossentropy}
\end{itemize}

\newpage
\section{Résultats et analyse}
Pour notre dataset, nous avons une répartition des données :
\begin{itemize}
    \item \textbf{Entraînement} : \texttt{(28 742, 50, 4)}
    \item \textbf{Évaluation} : \texttt{(7 186, 50, 4)}
\end{itemize}
Les performances globales obtenues à la fin de l'exécution du modèle :
\begin{center}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{} & \textbf{Valeur} \\ \hline
        Précision finale (\textit{Accuracy}) & \textbf{90,7\%} \\ \hline
        Précision de validation (\textit{Val\_Accuracy}) & \textbf{91,2\%} \\ \hline
    \end{tabular}
\end{center}

La précision de validation est un peu supérieure à la précision d'entraînement ; cela indique que le modèle n'est pas en sur-apprentissage (overfitting).

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/CourbeLSTM1.png}
    \caption{Courbe d'apprentissage du modèle LSTM (Accuracy et Loss)}
    \label{fig:courbe_lstm}
\end{figure}

\newpage
\subsection{Performance par catégorie}
\begin{itemize}
    \item \textbf{Disque} : Le F1-score est de 1.00. La signature des accès disque est très simple à repérer pour ce modèle, qui se trompe rarement.
    \item \textbf{CPU} : Un F1-score de 0.92. Le modèle n'a donc aucun problème à reconnaître les activités CPU.
    \item \textbf{Mémoire} : C'est la catégorie où le modèle a le plus de mal, avec un rappel (recall) de \textbf{0.59}. Une partie des activités mémoire est confondue avec le CPU.
\end{itemize}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/MatriceLSTM1.png}
    \caption{Matrice de confusion du modèle LSTM}
    \label{fig:matrice_lstm}
\end{figure}

Le tableau suivant détaille la précision, le rappel et le F1-score pour chaque classe :

\begin{center}
    \small
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Classe} & \textbf{Précision} & \textbf{Rappel} & \textbf{F1-score} & \textbf{Support} \\ \hline
        CPU & 0,86 & 0,97 & 0,91 & 3\,832 \\ \hline
        DISQUE & 1,00 & 0,99 & 1,00 & 1\,920 \\ \hline
        MÉMOIRE & 0,88 & 0,59 & 0,70 & 1\,434 \\ \hline
        \hline
        \textbf{Exactitude (Accuracy)} & \multicolumn{3}{c|}{\textbf{0,90}} & 7\,186 \\ \hline
        Macro moyenne & 0,91 & 0,85 & 0,87 & 7\,186 \\ \hline
        Moyenne pondérée & 0,90 & 0,90 & 0,89 & 7\,186 \\ \hline
    \end{tabular}
\end{center}

\subsection{Expérimentations et Optimisation}
Dans le but d'améliorer les performances du modèle LSTM, nous avons effectué plusieurs expérimentations.
\begin{itemize}
    \item \textbf{Taille de la fenêtre} : Est-ce que la taille de la fenêtre a une influence ?
    \item Test : 25 points ou 100 points.
    \item Observation : À 100 points, le modèle ne reconnaît pas la classe mémoire ; à 25 points, il y a du sur-apprentissage (overfitting).
\end{itemize}
\begin{itemize}
    \item \textbf{Taille des lots (Batch size)} : Est-ce que la taille des lots a une influence ?
    \item Test : 32 ou 128.
    \item Observation : À 32, le modèle confond davantage la mémoire et le CPU ; à 128, il y a plus d'erreurs et aucune amélioration.
\end{itemize}
\begin{itemize}
    \item \textbf{Dropout} : À partir de quel seuil observe-t-on du sur-apprentissage ?
    \item Test : 10\% ou 30\%.
    \item Observation : À 10\%, pas de difference ; à 30\%, on note un manque de données lors de l'apprentissage.
\end{itemize}

Ces tests confirment que nos valeurs initiales offrent le meilleur compromis entre rapidité d'exécution et précision.

\chapter{Modèle 2 : CNN 1D (Convolutional Neural Network)}

\section{Fonctionnement}
Le réseau de neurones convolutionnel (CNN) peut être utilisé sur des séries temporelles avec des convolutions 1D. Au lieu de regarder une série dans son intégralité, le modèle utilise des filtres pour détecter des motifs (pics, transitions brusques, etc.) sur la série temporelle. 

L'avantage du CNN par rapport au LSTM est sa capacité à repérer des signatures de formes précises sans avoir besoin de les mémoriser.

\section{Architecture}
Pour le modèle CNN 1D, nous avons défini l'architecture suivante :
\begin{itemize}
    \item \textbf{1re Couche Conv1D} : 64 filtres, une taille de noyau (kernel) de 3 ; pour repérer les motifs simples.
    \item \textbf{2e Couche Conv1D} : 128 filtres, une taille de noyau (kernel) de 3 ; pour repérer les combinaisons des motifs repérés par la couche précédente.
    \item \textbf{MaxPooling1D} : Réduit la dimension temporelle par 2, pour ne garder que les valeurs les plus importantes.
    \item \textbf{Dropout (20\%)} : Pour éviter le sur-apprentissage, 20\% des neurones sont désactivés aléatoirement durant l'entraînement.
    \item \textbf{Couche Dense} : 32 neurones ReLU pour la prise de décision.
    \item \textbf{Sortie} : 3 neurones Softmax pour distribuer une probabilité pour chacune des 3 classes (CPU, Disque, Mémoire).
\end{itemize}

L'entraînement est réalisé sur 15 epochs et batch size de 64.

Avec les paramètres suivants :
\begin{itemize}
    \item \textbf{Optimiseur} : Adam
    \item \textbf{Fonction de perte} : \texttt{sparse\_categorical\_crossentropy}
\end{itemize}

\newpage
\section{Résultats et analyse}
Pour notre dataset, nous avons une répartition des données :
\begin{itemize}
    \item \textbf{Entraînement} : \texttt{(28 742, 50, 4)}
    \item \textbf{Évaluation} : \texttt{(7 186, 50, 4)}
\end{itemize}
Les performances globales obtenues à la fin de l'exécution du modèle :
\begin{center}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{} & \textbf{Valeur} \\ \hline
        Précision finale (\textit{Accuracy}) & \textbf{91,6\%} \\ \hline
        Précision de validation (\textit{Val\_Accuracy}) & \textbf{91,8\%} \\ \hline
    \end{tabular}
\end{center}

La précision de validation est un peu supérieure à la précision d'entraînement ; cela indique que le modèle n'est pas en sur-apprentissage (overfitting).

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{capture/CourbeCNN1.png}
    \caption{Courbe d'apprentissage du modèle CNN 1D}
    \label{fig:courbe_cnn}
\end{figure}

\newpage
\subsection{Performance par catégorie}
\begin{itemize}
    \item \textbf{Disque} : Le F1-score est de 1.00. La signature des accès disque est très simple à repérer pour ce modèle.
    \item \textbf{CPU} : Un F1-score de 0.92. Le modèle n'a donc aucun problème à reconnaître les activités CPU.
    \item \textbf{Mémoire} : Ce modèle a egalment plus de mal a reperer les activiter mémoire avec un rappel (recall) de \textbf{0.60}. Une partie des activités mémoire est confondue avec le CPU.
\end{itemize}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/MatriceCNN1.png}
    \caption{Matrice de confusion du modèle LSTM}
    \label{fig:matrice_lstm}
\end{figure}

Le tableau suivant détaille la précision, le rappel et le F1-score pour chaque classe :

\begin{center}
    \small
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Classe} & \textbf{Précision} & \textbf{Rappel} & \textbf{F1-score} & \textbf{Support} \\ \hline
        CPU & 0,87 & 1,00 & 0,93 & 3\,832 \\ \hline
        DISQUE & 1,00 & 0,99 & 0,99 & 1\,920 \\ \hline
        MÉMOIRE & 0,99 & 0,60 & 0,75 & 1\,434 \\ \hline
        \hline
        \textbf{Exactitude (Accuracy)} & \multicolumn{3}{c|}{\textbf{0,92}} & 7\,186 \\ \hline
        Macro moyenne & 0,95 & 0,86 & 0,89 & 7\,186 \\ \hline
        Moyenne pondérée & 0,93 & 0,92 & 0,91 & 7\,186 \\ \hline
    \end{tabular}
\end{center}

\subsection{Expérimentations et Optimisation}
Dans le but d'améliorer les performances du modèle CNN, nous avons effectué plusieurs expérimentations.
\begin{itemize}
    \item \textbf{Taille de la fenêtre} : Est-ce que la taille de la fenêtre a une influence ? Faut-il plus de contexte ?
    \item Test : 25 points ou 100 points.
    \item Observation : Aucune influence
\end{itemize}
\begin{itemize}
    \item \textbf{Dropout} : À partir de quel seuil observe-t-on du sur-apprentissage ?
    \item Test : 10\% ou 30\%.
    \item Observation : À 10\%, aucune différence ; à 30\%, le rappel augmente de 0,01 à 0,02 point.
\end{itemize}
\begin{itemize}
    \item \textbf{Taille des noyaux} : Un noyau plus grand permet de repérer des motifs plus longs.
    \item Test : 5 ou 9.
    \item Observation : L'ajout d'une couche \texttt{BatchNormalization()} stabilise l'apprentissage. Avec une taille de 5, aucun changement n'est observé ; avec une taille de 9, le modèle commet plus d'erreurs lors de l'évaluation, ce qui indique un sur-apprentissage (overfitting).
\end{itemize}
\begin{itemize}
    \item \textbf{3e bloc} : Un bloc supplémentaire permet-il d'obtenir de meilleurs résultats ?
    \item Test : 256 filtres.
    \item Observation : Aucune amélioration de la précision n'est constatée, les confusions persistent. Cependant, une meilleure stabilité est obtenue.
\end{itemize}
\begin{itemize}
    \item \textbf{Taux d'apprentissage (Learning Rate)} : Configuration d'un taux d'apprentissage plus haut ou plus bas que la valeur par défaut.
    \item Test : 0,005 ou 0,0005.
    \item Observation : À 0,0005, les erreurs augmentent lors de l'évaluation ; à 0,005, aucun changement significatif n'est relevé.
\end{itemize}
\begin{itemize}
    \item \textbf{GAP} :
    \item Test : Remplacement de la couche \texttt{Flatten()} par \texttt{GlobalAveragePooling1D()}.
    \item Observation : Pas de changement significatif.
\end{itemize}
\begin{itemize}
    \item \textbf{Poids des erreurs} : Rendre les erreurs sur la classe mémoire plus punitives pour augmenter l'attention du modèle sur celle-ci.
    \item Test : \texttt{poids\_des\_classes = \{0: 1.0, 1: 1.0, 2: 2.0\}}.
    \item Observation : La précision générale diminue, mais elle augmente pour la classe mémoire. Le modèle prédit la classe mémoire plus souvent, mais perd en précision sur la classe CPU, qu'il confond désormais avec la mémoire.
\end{itemize}

Ces tests ont permis de définir la meilleure configuration, privilégiant la stabilité et la régularisation : un modèle à 2 ou 3 blocs avec \texttt{BatchNormalization}, un \textit{Dropout} de 0,3 et un noyau de taille 3 ou 5.

L'amélioration porte sur la classe \textbf{Mémoire} ; en rendant les erreurs plus punitives, le rappel passe à \textbf{0.69}, mais la précision générale diminue. La confusion avec le CPU est toujours présente. Les filtres de convolution semblent mieux repérer certaines signatures liées aux accès RAM que le mécanisme de mémoire du LSTM.

\chapter{Modèle 3 : Time Series Transformer}

\section{Fonctionnement}
Le Transformer est l'architecture qui utilise un mécanisme d'\textbf{Attention}. Le modele analyse simultanément tous les points de la fenetre et calcule un score d’importance par rapport à tous les autres points de la séquence

\section{Architecture}
L'architecture implémentée repose sur un bloc de codage, puis d'une tête de classification :

\begin{itemize}
    \item \textbf{Multi-Head Attention} : 4 têtes d'attention. Chaque tête apprend reperer un types de motifs différents.
    \item \textbf{Layer Normalization} : Stabilise l'apprentissage 
    \item \textbf{Feed-Forward Network (Conv1D)} : Deux couches de convolution avec un noyau de taille 1 qui fonctionne comme un réseau de neurones, pour les caractéristiques plus complexes.
    \item \textbf{Connexions Résiduelles} : Evite la dégradation des gradients et facilite l'entraînement du modèles.
    \item \textbf{Global Average Pooling 1D} : Compresse la séquence en un vecteur unique., qui sera utilisé pour la décision finale.
    \item \textbf{Couches Denses} : Une série de couches de neurone termine le traitement avant le \textbf{Softmax} à 3 classes.
\end{itemize}

\newpage
\section{Résultats et analyse}
Pour notre dataset, nous avons une répartition des données comme les modèles précédents. Les resulats obtenues pour le Transformer sont :
\begin{center}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Métrique} & \textbf{Valeur} \\ \hline
        Précision finale (\textit{Accuracy}) & \textbf{89,1\%} \\ \hline
        Précision de validation (\textit{Val\_Accuracy}) & \textbf{89,5\%} \\ \hline
    \end{tabular}
\end{center}
Absence de sur-apprentissage confirméegrace a la proximité des scores d'entraînement et de validation. Le modèle est légèrement moins performant que le CNN 1D.
\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/CourbeTransformer1.png}
    \caption{Courbe d'apprentissage du modèle Transformer}
    \label{fig:courbe_transformer}
\end{figure}

\newpage
\subsection{Performance par catégorie}
L'analyse détaillée montre que le Transformer partage les mêmes difficultés que le LSTM :
\begin{itemize}
    \item \textbf{Disque} : F1-score de 1.00. Les signatures sont très bien identifiées.
    \item \textbf{CPU} : F1-score de 0.90. 
    \item \textbf{Mémoire} : Comme pour les autres modèles, où il y a le plus d'erreur avec un F1-score de 0.71.
\end{itemize}
\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{capture/MatriceTransformer1.png}
    \caption{Matrice de confusion du modèle Transformer}
    \label{fig:matrice_transformer}
\end{figure}
Le tableau suivant détaille la précision, le rappel et le F1-score pour chaque classe :
\begin{center}
    \small
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Classe} & \textbf{Précision} & \textbf{Rappel} & \textbf{F1-score} & \textbf{Support} \\ \hline
        CPU & 0,86 & 0,98 & 0,92 & 3\,832 \\ \hline
        DISQUE & 1,00 & 0,99 & 0,99 & 1\,920 \\ \hline
        MÉMOIRE & 0,92 & 0,58 & 0,71 & 1\,434 \\ \hline
        \hline
        \textbf{Exactitude (Accuracy)} & \multicolumn{3}{c|}{\textbf{0,90}} & 7\,186 \\ \hline
        Macro moyenne & 0,93 & 0,85 & 0,87 & 7\,186 \\ \hline
        Moyenne pondérée & 0,91 & 0,90 & 0,90 & 7\,186 \\ \hline
    \end{tabular}
\end{center}

\subsection{Expérimentations et Optimisation}
Plusieurs variantes ont été testées pour optimiser ce modèle :

\begin{itemize}
    \item \textbf{Empilement (Stacking)} : Créer plus de blocs \texttt{transformer\_encoder}.
    \item Test : Ajout de 1 ou 2 blocs.
    \item Observation : Pas d'amélioration significative.
    \item \textbf{Dimension} : Ajuster \texttt{head\_size} et \texttt{ff\_dim}.
    \item Test : Passage à 128.
    \item Observation : En passant à 128, le modèle n'a pas gagné en précision mais est devenu un peu plus stable.
    \item \textbf{Nombre de têtes} : Est-ce que le nombre de têtes a une influence ?
    \item Test : 8 têtes.
    \item Observation : Augmenter le nombre de têtes n'a pas amélioré la précision ; le modèle a commencé à présenter du sur-apprentissage (overfitting).
    \item \textbf{GAP} :
    \item Test : Remplacer ou ajouter une couche \texttt{GlobalMaxPooling1D}.
    \item Observation : En remplaçant le GAP par du \texttt{GlobalMaxPooling1D}, le modèle a perdu en précision lors de l'évaluation.
    \item \textbf{Taux d'apprentissage (Learning Rate)} :
    \item Test : 0,005 ou 0,0005.
    \item Observation : Pas d'amélioration significative.
    \item \textbf{Poids des erreurs} : Rendre les erreurs sur la classe mémoire plus punitives pour augmenter l'attention du modèle sur celle-ci.
    \item Test : \texttt{poids\_des\_classes = \{0: 1.0, 1: 1.0, 2: 2.0\}}.
    \item Observation : Résultats identiques à ceux du modèle CNN.
\end{itemize}

Tous ces tests montrent que l'architecture Transformer a atteint son plus haut taux de performance avec le jeu de données actuel. L'augmentation de la complexité du modèle n'a pas apporté d'amélioration significative de la précision et a même montré du sur-apprentissage (\textit{overfitting}). 

L'utilisation du \textit{Global Average Pooling} était un meilleur choix que le \textit{Global Max Pooling}, ce qui confirme que la moyenne globale des caractéristiques temporelles est plus efficace pour identifier les signatures.

\chapter{Expérimentation : Une variable supplémentaire}
\section{Ajout de la variable "Energy Usage"}
Nous avons utilisé quatre variables (CPU, RAM, Lectures et Écritures Disque).Étant donné que tous les modèles présentaient des difficultés à distinguer le CPU de la mémoire, l'hypothèse d'un manque de données a été formulée. Afin d'améliorer la distinction entre les classes, une cinquième variable a été ajoutée : \texttt{Energy\_usage\_during\_time}.
La consommation énergétique est supposée suivre des signatures spécifiques selon le composant sollicité.

\section{Résultats et Comparaisons}
Les trois modèles (LSTM, CNN 1D et Transformer) ont été réentraînés avec cette donnée supplémentaire. Les résultats indiquent une amélioration de la précision pour l'ensemble des modèles.
\begin{figure}[htbp]
    \centering

    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{capture/CourbeLSTM2.png}
        \label{fig:courbe_lstm}
    \end{minipage}
    \hfill 
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{capture/MatriceLSTM2.png}
        \label{fig:matrice_lstm}
    \end{minipage}
    
    \vspace{10pt} 
    \caption{Résultats LSTM - Avec variable Énergie}
    \label{fig:resultats_complets}
\end{figure}

\begin{figure}[htbp]
    \centering

    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{capture/CourbeCNN2.png}
        \label{fig:courbe_cnn}
    \end{minipage}
    \hfill 
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{capture/MatriceCNN2.png}
        \label{fig:matrice_cnn}
    \end{minipage}
    
    \vspace{10pt} 
    \caption{Résultats CNN 1D - Avec variable Énergie}
    \label{fig:resultats_complets}
\end{figure}

\begin{figure}[htbp]
    \centering

    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{capture/CourbeTransformer2.png}
        \label{fig:courbe_transformer}
    \end{minipage}
    \hfill 
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{capture/MatriceTransformer2.png}
        \label{fig:matrice_transformer}
    \end{minipage}
    
    \vspace{10pt} 
    \caption{Résultats Transformer - Avec variable Énergie}
    \label{fig:resultats_complets}
\end{figure}

\newpage
\section{Analyse Résultat}

\begin{table}[h]
\centering
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Modèle} & \textbf{Accuracy} & \textbf{F1-Score CPU} & \textbf{F1-Score DISK} & \textbf{F1-Score MEM} \\ \hline
LSTM            & 93\%              & 0,94                  & 1,00                   & 0,81                  \\ \hline
CNN 1D          & \textbf{94\%}     & 0,94                  & 1,00                   & \textbf{0,83}         \\ \hline
Transformer     & \textbf{94\%}     & 0,94                  & 1,00                   & \textbf{0,83}         \\ \hline
\end{tabular}
\caption{Comparaison des performances des modèles avec 5 variables.}
\label{tab:comparaison_5_vars}
\end{table}


L'analyse des rapports de classification a permis d'éclairer certains points suite à l'ajout de la variable \texttt{Energy\_usage\_during\_time} :

\begin{itemize}
    \item \textbf{Séparabilité parfaite du disque} : Les trois modèles atteignent désormais un score de 1,00 (précision et rappel) sur la classe DISQUE.
    \item \textbf{Amélioration de la classe mémoire} : La classe MÉMOIRE reste la plus difficile à identifier. Le rappel est passé à 79 \% pour le CNN et le Transformer, réduisant ainsi la confusion avec la charge CPU.
    \item \textbf{Convergence des performances} : Le CNN 1D et le Transformer affichent des résultats similaires (94 \% d'accuracy). Le modèle LSTM est toujours légèrement en dessous, même en augmentant le nombre d'époques à 25 (92 \% d'accuracy).
\end{itemize}

En conclusion, la précision globale a augmenté sur tous les modèles. L'ajout de la variable sur la consommation électrique s'est révélé être un élément essentiel pour différencier le CPU de la MÉMOIRE.


\chapter{Perspectives}
Pour une utilisation réelle, capable d'identifier une application à partir d'une trace d'exécution (navigateur, compilateur, traitement de texte), plusieurs pistes sont possibles :

\begin{itemize}
    \item \textbf{Fenêtres dynamiques} : Actuellement, les modèles utilisent des segments de données définis. En situation réelle, le système devra traiter un flux continu. Il faudrait alors rendre les fenêtres dynamiques ; cela permettrait d'analyser la signature en temps réel et de détecter les différentes étapes d'exécution.
    \item \textbf{Architecture} : Une approche possible serait d'utiliser une architecture de modèle à deux niveaux. Un premier modèle (comme réalisé dans ce projet) identifierait les séquences principales (CPU, Disque, Mémoire). Ses résultats serviraient d'entrées à un deuxième modèle, utilisé pour reconnaître l'enchaînement de ces phases afin de déterminer le type d'application.
\end{itemize}


\end{document}
